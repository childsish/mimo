#include "mimo/Engine.h"

template<typename T, typename P>
void mimo::Engine::register_step<T>(std::shared_ptr<workflow::Step> identifier, P &&... args) {

}

void mimo::Engine::run(const workflow::Workflow &workflow) {

}
